import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 600px;
  background-color: orange;
`;
export default function LayoutSidebar() {
  return <Wrapper>여기는 사이드바 영역 입니다.</Wrapper>;
}
